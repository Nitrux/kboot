# Default values for booting a kernel with kboot
# 2023 (c) Nitrux Latinoamericana S.C.

# Define the path of vmlinuz and initrd to use with kexec.
# We suggest using the directory /kboot to organize kernels.
# Regular files and soft links are valid file types to use.
#
KEXEC_VMLINUZ=/kboot/debian/vmlinuz
KEXEC_INITRD=/kboot/debian/initrd.img

# Define the parameters used when loading the kernel.
# Kernel parameters go as a single line.
#
# Below is a list parameters used in Nitrux:
#
# apparmor=1
# security=apparmor
# audit=0
# intel_pstate=disable
# hpet=disable
# rcupdate.rcu_expedited=1
# libahci.ignore_sss=1
# elevator=noop
# radeon.cik_support=0
# amdgpu.cik_support=1
# radeon.si_support=0
# amdgpu.si_support=1
# nvidia_drm.modeset=1
# overlayroot=tmpfs:swap=1,recurse=0
# zswap.enabled=1
# zswap.compressor=lz4
# zswap.max_pool_percent=20
# zswap.zpool=z3fold
# nvme_core.multipath=Y
# quiet
#
# NOTE: Do not use undeclared variables in the parameters. Doing so will prevent the kernel from loading. This is not a bug.
#
# NOTE: Do not forget to include the root parameter. Otherwise, after running kboot, you will be staring at the initramfs. Use the command: cat /proc/cmdline to view the UUID.
# For example: root=UUID=xxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx
#
KERNEL_PARAMETERS="quiet amdgpu.cik_support=1 amdgpu.si_support=1 apparmor=1 elevator=noop libahci.ignore_sss=1 nvidia_drm.modeset=1 nvme_core.multipath=Y overlayroot=tmpfs:swap=1,recurse=0 radeon.cik_support=0 radeon.si_support=0 security=apparmor"
